Проблема в том, что в вашем коде есть кнопка и цветовой блок, но при клике на кнопку срабатывает только alert, а цвет не меняется. Вот исправленная версия:

## Исправленный код

**index.html** (остается без изменений)

**style.css** (добавляем класс для изменения цвета):
```css
.container {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.button {
    padding: 20px 40px;
    background: #007bff;
    color: white;
    border: none;
    cursor: pointer;
    border-radius: 5px;
    font-size: 16px;
}

.button:hover {
    background: #0056b3;
}

.color-box {
    width: 200px;
    height: 200px;
    background: #f8f9fa;
    margin-top: 20px;
    display: flex;
    justify-content: center;
    align-items: center;
    border: 2px solid #ddd;
    border-radius: 8px;
    transition: background-color 0.3s ease;
}

/* Новый класс для изменения цвета */
.color-box.changed {
    background: #ff6b6b;
    color: white;
}
```

**script.js** (добавляем функционал изменения цвета):
```javascript
document.addEventListener('DOMContentLoaded', () => {
    const button = document.querySelector('.button');
    const colorBox = document.querySelector('.color-box');
    
    button.addEventListener('click', () => {
        // alert('Привет от DevTools!');
        
        // Переключаем класс для изменения цвета
        colorBox.classList.toggle('changed');
        
        // Также меняем текст внутри блока
        const paragraph = colorBox.querySelector('p');
        if (colorBox.classList.contains('changed')) {
            paragraph.textContent = 'Цвет изменен!';
        } else {
            paragraph.textContent = 'Измени мой цвет!';
        }
    });
});
```

## Альтернативный вариант с рандомными цветами:

**script.js** (более динамичный вариант):
```javascript
document.addEventListener('DOMContentLoaded', () => {
    const button = document.querySelector('.button');
    const colorBox = document.querySelector('.color-box');
    const paragraph = colorBox.querySelector('p');
    
    // Массив случайных цветов
    const colors = ['#ff6b6b', '#4ecdc4', '#45b7d1', '#96ceb4', '#feca57', '#ff9ff3'];
    
    button.addEventListener('click', () => {
        // Генерируем случайный цвет
        const randomColor = colors[Math.floor(Math.random() * colors.length)];
        
        // Применяем цвет к блоку
        colorBox.style.backgroundColor = randomColor;
        paragraph.textContent = `Цвет: ${randomColor}`;
        paragraph.style.color = '#333';
    });
});
```

## Как использовать DevTools для отладки:

1. **Откройте DevTools**: F12 или правой кнопкой → "Просмотреть код"
2. **Вкладка Elements**: Просмотр и изменение HTML/CSS в реальном времени
3. **Console**: Просмотр ошибок JavaScript
4. **Sources**: Отладка JavaScript кода
5. **Network**: Мониторинг сетевых запросов

## Практические задания с DevTools:

1. **Изменение CSS в реальном времени**:
   - Найдите `.color-box` в Elements
   - Измените background-color прямо в стилях
   - Добавьте новые CSS свойства

2. **Отладка JavaScript**:
   - Во вкладке Sources поставьте точку остановки в обработчике клика
   - Проанализируйте выполнение кода по шагам

3. **Работа с Console**:
   ```javascript
   // Проверьте элементы
   console.log(document.querySelector('.color-box'));
   
   // Протестируйте функцию изменения цвета
   document.querySelector('.color-box').style.backgroundColor = 'red';
   ```

Теперь при клике на кнопку цвет блока будет меняться, и вы сможете использовать DevTools для анализа и отладки кода!